server:
  port: 8888
cors:
  originPatterns: http://localhost:8081,http://outra.origem.exemplo.com
spring:
  application:
    name: Loja Ceda Artes

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ceda_arte?useTimezone=true&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: create #mudar para none depois
    show-sql: true

spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true

    #logging:
    # level:
    #  root: INFO

    password: admin123
    #br.com.fco_romario.loja_ceda_artes: DEBUG
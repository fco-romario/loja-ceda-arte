spring:
  application:
    name: Loja Ceda Artes
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ceda-arte?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
    jpa:
      hibernate:
        ddl-auto: update #mudar para none depois
      show-sql: true